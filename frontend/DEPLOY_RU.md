# 🚀 Быстрый деплой фронтенда на Vercel

## ⚠️ ВАЖНО: Сначала разверните бэкенд!

Фронтенд не будет работать без развёрнутого бэкенда. Варианты развёртывания бэкенда:
- **Railway** (бесплатно, рекомендуется)
- **Render** (бесплатно)
- **Fly.io**
- **VPS**

После развёртывания бэкенда у вас будет URL типа: `https://your-backend.railway.app`

---

## 📝 Шаги развёртывания

### Вариант 1: Через Vercel CLI (рекомендуется)

#### 1. Установите Vercel CLI

```bash
npm install -g vercel
```

#### 2. Войдите в аккаунт

```bash
vercel login
```

#### 3. Деплой

```bash
cd /Users/erboltalipov/myapps/ah/frontend
vercel
```

Отвечайте на вопросы:
- `Set up and deploy?` → **Yes**
- `Which scope?` → Выберите ваш аккаунт
- `Link to existing project?` → **No**
- `What's your project's name?` → **erp-lite** (или любое имя)
- `In which directory is your code located?` → **./`

#### 4. Добавьте URL бэкенда

```bash
vercel env add API_BASE_URL production
```

Введите URL вашего бэкенда, например:
```
https://your-backend.railway.app/api
```

#### 5. Деплой в продакшен

```bash
vercel --prod
```

✅ Готово! Откройте URL который покажет Vercel.

---

### Вариант 2: Через Web интерфейс Vercel

#### 1. Создайте аккаунт на [vercel.com](https://vercel.com)

#### 2. Создайте GitHub репозиторий

```bash
cd /Users/erboltalipov/myapps/ah
git init
git add .
git commit -m "Initial commit"
# Создайте репозиторий на GitHub и выполните:
git remote add origin https://github.com/ваш-username/erp-lite.git
git push -u origin main
```

#### 3. Импортируйте проект в Vercel

1. Зайдите на [vercel.com/new](https://vercel.com/new)
2. Выберите ваш GitHub репозиторий
3. В настройках:
   - **Root Directory**: `frontend`
   - **Framework Preset**: Other
   - Оставьте остальное по умолчанию

4. Перед деплоем нажмите **Environment Variables**:
   - **Name**: `API_BASE_URL`
   - **Value**: `https://your-backend.railway.app/api`
   - Выберите все окружения (Production, Preview, Development)

5. Нажмите **Deploy**

✅ Готово! После деплоя откроется URL вашего приложения.

---

## 🔧 Настройка CORS на бэкенде

После получения Vercel URL (например `https://erp-lite.vercel.app`):

### Если у вас есть `.env` файл:

Добавьте в `.env`:
```env
CORS_ORIGINS=https://your-app.vercel.app,http://localhost:3000
```

### Если используете прямую настройку в коде:

В `backend/main.py` измените:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-app.vercel.app",  # ваш Vercel URL
        "http://localhost:3000",          # для локальной разработки
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

⚠️ **Перезапустите бэкенд** после изменения CORS!

---

## ✅ Проверка

1. Откройте ваш Vercel URL
2. Попробуйте **зарегистрировать** нового пользователя
3. **Войдите** в систему
4. Проверьте что все страницы работают

---

## 🐛 Если что-то не работает

### Ошибка CORS

**Проблема**: `Access to fetch has been blocked by CORS policy`

**Решение**:
1. Проверьте что CORS на бэкенде включает ваш Vercel URL
2. Перезапустите бэкенд
3. Очистите кэш браузера (Ctrl+Shift+R)

### Ошибка "Failed to fetch"

**Проблема**: Не удаётся подключиться к API

**Решение**:
1. Проверьте что бэкенд работает (откройте `https://your-backend.com/docs`)
2. Проверьте переменную `API_BASE_URL` в Vercel:
   - Settings → Environment Variables
3. Убедитесь что URL правильный (должен заканчиваться на `/api`)

### Проверка конфигурации

Откройте консоль браузера (F12) и выполните:

```javascript
console.log(window.ENV)
console.log(window.APP_CONFIG)
```

Должно показать ваш API URL.

---

## 📞 Нужна помощь с развёртыванием бэкенда?

Дайте знать какой сервис хотите использовать:
- **Railway** — проще всего, бесплатный tier
- **Render** — бесплатный tier, но медленнее
- **Fly.io** — бесплатный tier
- **VPS** — если есть свой сервер

Могу помочь с пошаговой инструкцией!
