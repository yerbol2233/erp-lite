# Развёртывание фронтенда ERP-Lite на Vercel

## 📋 Предварительные требования

1. **Развёрнутый бэкенд** — у вас должен быть работающий API с публичным URL
2. **Аккаунт на Vercel** — зарегистрируйтесь на [vercel.com](https://vercel.com)
3. **Git репозиторий** (опционально, но рекомендуется)

## 🚀 Способ 1: Через Vercel CLI (быстрый)

### 1. Установка Vercel CLI

```bash
npm install -g vercel
```

### 2. Логин в Vercel

```bash
vercel login
```

### 3. Деплой из директории frontend

```bash
cd /Users/erboltalipov/myapps/ah/frontend
vercel
```

При первом деплое Vercel задаст вопросы:
- **Set up and deploy?** → Yes
- **Which scope?** → Your account
- **Link to existing project?** → No
- **What's your project's name?** → erp-lite (или любое другое имя)
- **In which directory is your code located?** → ./
- **Want to override settings?** → No

### 4. Настройка переменных окружения

После деплоя добавьте переменную окружения:

```bash
vercel env add API_BASE_URL
```

Введите URL вашего бэкенда, например: `https://your-backend.railway.app/api`

Затем передеплойте:

```bash
vercel --prod
```

## 🔗 Способ 2: Через GitHub + Vercel Dashboard

### 1. Создайте Git репозиторий

```bash
cd /Users/erboltalipov/myapps/ah
git init
git add .
git commit -m "Initial commit"
```

### 2. Загрузите на GitHub

1. Создайте новый репозиторий на GitHub
2. Следуйте инструкциям GitHub для push'а:

```bash
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

### 3. Подключите к Vercel

1. Перейдите на [vercel.com/dashboard](https://vercel.com/dashboard)
2. Нажмите **Add New** → **Project**
3. Выберите ваш GitHub репозиторий
4. В настройках проекта:
   - **Framework Preset**: Other
   - **Root Directory**: `frontend`
   - **Build Command**: `bash generate-env.sh` (опционально)
   - **Output Directory**: `.` (текущая директория)

### 4. Добавьте переменные окружения

В настройках проекта Vercel:
1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте:
   - **Key**: `API_BASE_URL`
   - **Value**: URL вашего бэкенда (например, `https://your-backend.railway.app/api`)
   - **Environments**: Production, Preview, Development

3. Нажмите **Save**

### 5. Передеплойте

Vercel автоматически передеплоит проект когда вы сохраните переменные окружения.

## 🔧 Настройка бэкенда

### Обновите CORS на бэкенде

После того как получите URL Vercel (например, `https://erp-lite.vercel.app`), обновите CORS на бэкенде.

Если в `.env` файле бэкенда нет этой переменной, добавьте:

```env
CORS_ORIGINS=https://erp-lite.vercel.app,http://localhost:3000
```

Или в `main.py` обновите:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://erp-lite.vercel.app",  # ваш Vercel URL
        "http://localhost:3000",         # для локальной разработки
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## ✅ Проверка работоспособности

После деплоя:

1. **Откройте ваш Vercel URL**
2. **Попробуйте зарегистрироваться**
   - Если видите ошибку CORS — проверьте настройки бэкенда
   - Если видите "Connection refused" — проверьте что бэкенд доступен
3. **Попробуйте войти**
4. **Проверьте навигацию** по разделам платформы

## 🐛 Отладка проблем

### Проблема: "Failed to fetch" или CORS error

**Решение:**
1. Откройте DevTools (F12) → Console
2. Проверьте какой URL используется для запросов
3. Убедитесь что бэкенд доступен по этому URL
4. Проверьте CORS настройки на бэкенде

### Проблема: API_BASE_URL не подставляется

**Решение:**
1. Проверьте что переменная окружения добавлена в Vercel
2. Передеплойте проект
3. Проверьте в браузере (F12 → Console):
   ```javascript
   console.log(window.ENV)
   console.log(window.APP_CONFIG)
   ```

### Проблема: 404 при перезагрузке страницы

**Решение:** Это уже решено через `vercel.json` с настройкой rewrites.

## 📝 Следующие шаги

1. **Развернуть бэкенд** (если ещё не развёрнут)
2. **Настроить продакшен БД** (PostgreSQL вместо SQLite)
3. **Добавить мониторинг** и логирование
4. **Настроить custom domain** (опционально)

## 🔗 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vercel CLI](https://vercel.com/docs/cli)
- [Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
