# 🚀 Деплой на Render.com за 3 шага (5 минут)

## ✅ Что уже готово:
- Создан `render.yaml` — конфигурация для автоматического деплоя
- `Procfile`, `runtime.txt`, `requirements.txt` — все на месте

---

## 📋 Что нужно сделать (3 шага):

### Шаг 1: Загрузить код на GitHub (если ещё не загружен)

```bash
cd /Users/erboltalipov/myapps/ah
git add .
git commit -m "Add Render.com configuration"
git push origin main
```

> Если git не настроен, пропустите — можете загрузить через веб-интерфейс GitHub

---

### Шаг 2: Зарегистрироваться на Render.com

1. **Перейдите:** https://dashboard.render.com/register
2. **Выберите:** "Sign up with Email" (НЕ GitHub!)
3. **Введите email и пароль**
4. **Подтвердите email** (проверьте почту)

---

### Шаг 3: Создать проект одной кнопкой

#### Вариант A: Через Blueprint (автоматически)

1. **Перейдите:** https://dashboard.render.com/select-repo?type=blueprint

2. **Подключите GitHub репозиторий:**
   - Нажмите "Connect GitHub"
   - Выберите репозиторий: `idgafenot/erp`
   - Нажмите "Connect"

3. **Render автоматически:**
   - Найдёт `render.yaml`
   - Создаст PostgreSQL базу
   - Создаст веб-сервис для бэкенда
   - Настроит переменные окружения
   - Начнёт деплой

4. **Подождите ~5-7 минут** пока происходит деплой

5. **Получите URL** (например: `https://erp-lite-backend.onrender.com`)

#### Вариант B: Вручную (если Blueprint не работает)

1. **Зайдите:** https://dashboard.render.com

2. **Создайте PostgreSQL:**
   - New → PostgreSQL
   - Name: `erp-lite-db`
   - Plan: Free
   - Create Database

3. **Создайте Web Service:**
   - New → Web Service
   - Connect your GitHub repo: `idgafenot/erp`
   - Name: `erp-lite-backend`
   - Region: Oregon
   - Branch: `main`
   - Root Directory: `backend`
   - Runtime: Python 3
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`

4. **Добавьте Environment Variables:**
   - `SECRET_KEY` = `BqdjrhXV7S1OTdN5cWAe6h2MR9BPzPoi-a2dduAvxPA`
   - `DEBUG` = `False`
   - `CORS_ORIGINS` = `*`
   - `DATABASE_URL` = (скопируйте из созданной PostgreSQL базы → "Internal Database URL")

5. **Нажмите "Create Web Service"**

---

## ✅ Проверка

После деплоя:

1. **Откройте:** `https://ваш-сервис.onrender.com/docs`
2. **Должна открыться** Swagger документация API

**Скопируйте URL** — он понадобится для Vercel!

---

## ⚠️ Важно знать о Free плане Render.com

- ✅ **Бесплатно навсегда**
- ⏱️ **Засыпает после 15 минут неактивности** (первый запрос будет медленным ~30 сек)
- 🔄 **Пробуждается автоматически** при первом запросе
- 💾 **PostgreSQL** — 90 дней хранения на Free плане

---

## 🐛 Возможные проблемы

### Деплой падает с ошибкой

**Проверьте логи:**
1. В Render Dashboard → ваш сервис → Logs
2. Посмотрите что именно падает

**Частые проблемы:**
- Не установлены зависимости → проверьте `requirements.txt`
- Неправильный путь → проверьте Root Directory: `backend`

### База данных не подключается

**Убедитесь:**
1. DATABASE_URL правильно скопирован
2. Используется Internal Database URL (не External)

---

## 📞 После деплоя

Когда бэкенд будет развёрнут, дайте знать:
1. **URL бэкенда** (например: `https://erp-lite-backend.onrender.com`)
2. Я помогу развернуть фронтенд на Vercel с этим URL!

---

**Удачи! Это займёт 5-10 минут** ⚡
