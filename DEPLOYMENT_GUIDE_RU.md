# ✅ Полная инструкция по развёртыванию ERP-Lite

## 🎯 Цель
Развернуть фронтенд на Vercel и бэкенд на Railway так, чтобы можно было:
- ✅ Регистрироваться
- ✅ Авторизоваться
- ✅ Работать с платформой

---

## 📦 Шаг 1: Развёртывание бэкенда на Railway

### 1.1. Подготовка (уже сделано ✅)
- [x] Создан `runtime.txt` с версией Python
- [x] Создан `Procfile` для запуска приложения
- [x] Добавлен PostgreSQL драйвер в `requirements.txt`

### 1.2. Регистрация на Railway
1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **Start a New Project**

### 1.3. Деплой бэкенда

**Вариант A: Через GitHub (рекомендуется)**

1. Создайте Git репозиторий (если ещё нет):
   ```bash
   cd /Users/erboltalipov/myapps/ah
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. Создайте репозиторий на GitHub и загрузите код:
   ```bash
   # Замените на ваш URL
   git remote add origin https://github.com/ваш-username/erp-lite.git
   git branch -M main
   git push -u origin main
   ```

3. В Railway:
   - Нажмите **New Project** → **Deploy from GitHub repo**
   - Выберите ваш репозиторий
   - Railway начнёт деплой

4. Настройте Root Directory:
   - Откройте проект в Railway
   - Перейдите в **Settings** → **General**
   - В **Root Directory** укажите: `backend`
   - Сохраните

**Вариант B: Через Railway CLI**

1. Установите CLI:
   ```bash
   npm install -g @railway/cli
   # или
   brew install railway
   ```

2. Войдите и деплойте:
   ```bash
   cd /Users/erboltalipov/myapps/ah/backend
   railway login
   railway init
   railway up
   ```

### 1.4. Добавьте PostgreSQL

1. В проекте Railway нажмите **New** → **Database** → **Add PostgreSQL**
2. Railway автоматически свяжет БД с приложением
3. Переменная `DATABASE_URL` будет добавлена автоматически

### 1.5. Настройте переменные окружения

В проекте Railway → **Variables** добавьте:

```
SECRET_KEY=сгенерируйте-случайный-ключ-32-символа
DEBUG=False
```

Для генерации SECRET_KEY:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 1.6. Получите URL бэкенда

После успешного деплоя:
1. Перейдите в **Settings** → **Networking**
2. Нажмите **Generate Domain**
3. Скопируйте URL (например: `https://erp-lite-backend.up.railway.app`)

**❗️ ВАЖНО:** Сохраните этот URL, он понадобится для фронтенда!

---

## 🌐 Шаг 2: Развёртывание фронтенда на Vercel

### 2.1. Подготовка (уже сделано ✅)
- [x] Создан `vercel.json` для маршрутизации
- [x] Создан `config.js` для переменных окружения
- [x] Обновлён `api.js` для использования переменных окружения

### 2.2. Регистрация на Vercel
1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Подтвердите доступ к репозиториям

### 2.3. Деплой фронтенда

**Вариант A: Через Web интерфейс (рекомендуется)**

1. На [vercel.com/new](https://vercel.com/new) нажмите **Import Project**
2. Выберите ваш GitHub репозиторий
3. В настройках проекта:
   - **Project Name**: `erp-lite` (или другое имя)
   - **Root Directory**: `frontend`
   - **Framework Preset**: Other
   - **Build Command**: (оставить пустым)
   - **Output Directory**: `.`

4. **Добавьте переменную окружения** (важно!):
   - Откройте **Environment Variables**
   - Добавьте:
     - **Name**: `API_BASE_URL`
     - **Value**: `https://ваш-railway-url.up.railway.app/api`
     - Environments: Production, Preview, Development (выбрать все)

5. Нажмите **Deploy**

**Вариант B: Через Vercel CLI**

1. Установите CLI:
   ```bash
   npm install -g vercel
   ```

2. Войдите и деплойте:
   ```bash
   cd /Users/erboltalipov/myapps/ah/frontend
   vercel login
   vercel
   ```

3. Добавьте переменную окружения:
   ```bash
   vercel env add API_BASE_URL production
   # Введите URL: https://ваш-railway-url.up.railway.app/api
   ```

4. Деплой в продакшен:
   ```bash
   vercel --prod
   ```

### 2.4. Получите URL фронтенда

После деплоя Vercel покажет URL типа:
```
https://erp-lite.vercel.app
```

---

## 🔧 Шаг 3: Настройка CORS на бэкенде

Теперь нужно разрешить фронтенду обращаться к бэкенду.

### 3.1. Обновите переменные окружения в Railway

В проекте Railway → **Variables** добавьте:

```
CORS_ORIGINS=https://ваш-vercel-url.vercel.app,http://localhost:3000
```

Замените `ваш-vercel-url.vercel.app` на реальный URL вашего Vercel приложения.

### 3.2. Перезапустите бэкенд

В Railway:
1. Перейдите в **Deployments**
2. Нажмите на три точки у последнего деплоя
3. Выберите **Restart**

Или просто сделайте любой коммит в репозиторий (если используете GitHub integration).

---

## ✅ Шаг 4: Проверка работоспособности

### 4.1. Проверьте бэкенд

1. Откройте в браузере: `https://ваш-railway-url.up.railway.app/docs`
2. Должна открыться Swagger документация
3. Попробуйте эндпоинт `/health` — должен вернуть `{"status": "healthy"}`

### 4.2. Проверьте фронтенд

1. Откройте ваш Vercel URL: `https://ваш-vercel-url.vercel.app`
2. Должна открыться страница авторизации

### 4.3. Протестируйте регистрацию

1. Перейдите на вкладку **Регистрация**
2. Заполните форму:
   - Имя: `Test User`
   - Email: `test@example.com`
   - Пароль: `password123`
3. Нажмите **Зарегистрироваться**
4. Если всё работает — вас перенаправит в систему

### 4.4. Протестируйте вход

1. Выйдите из системы
2. Войдите с теми же данными
3. Проверьте что можете переходить по разделам:
   - Дашборд
   - Клиенты
   - Товары
   - Заказы
   - Платежи

---

## 🐛 Решение проблем

### ❌ Ошибка CORS

**Симптом**: `Access to fetch has been blocked by CORS policy`

**Решение**:
1. Убедитесь что `CORS_ORIGINS` в Railway содержит ваш Vercel URL
2. Перезапустите бэкенд в Railway
3. Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### ❌ Ошибка "Failed to fetch"

**Симптом**: Не удаётся подключиться к API

**Решение**:
1. Проверьте что бэкенд работает (откройте `/docs`)
2. Проверьте `API_BASE_URL` в Vercel:
   - Settings → Environment Variables
   - Должен быть вида: `https://....railway.app/api` (с `/api` на конце!)
3. После изменения переменной — передеплойте в Vercel

### ❌ Не работает вход/регистрация

**Проверьте в консоли браузера (F12 → Console)**:

```javascript
console.log(window.ENV)
console.log(window.APP_CONFIG)
```

Должно показать правильный API URL.

**Проверьте во вкладке Network (F12 → Network)**:
1. Попробуйте зарегистрироваться
2. Посмотрите на запрос к API
3. Если 4xx/5xx ошибка — посмотрите детали

---

## 📝 Краткая справка по URL

После развёртывания у вас будет:

| Сервис | URL | Назначение |
|--------|-----|------------|
| Бэкенд Railway | `https://xxx.up.railway.app` | API сервер |
| Бэкенд Docs | `https://xxx.up.railway.app/docs` | Swagger документация |
| Фронтенд Vercel | `https://xxx.vercel.app` | Веб-интерфейс |

---

## 🎉 Готово!

Теперь ваша ERP-система полностью развёрнута и работает в облаке!

**Следующие шаги:**
- Настройте custom domain (опционально)
- Добавьте SSL сертификат (если нужно)
- Настройте мониторинг и алерты
- Сделайте бэкап базы данных

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи Railway: **Deployments** → **View Logs**
2. Проверьте логи Vercel: **Deployments** → **Building/Deploying**
3. Проверьте консоль браузера (F12)

Удачи! 🚀
