# 📊 ERP-Lite — Система автоматизации бизнеса

ERP-Lite — это легковесная система для автоматизации учета заказов, клиентов и товаров, разработанная на стеке Python (FastAPI) и Vanilla JS.

## 🚀 Как протестировать платформу

### 1. Подготовка окружения
Убедитесь, что у вас установлен **Python 3.10+** (проект тестировался на 3.14).

```bash
# Перейдите в папку backend
cd backend

# Создайте виртуальное окружение (если еще не создано)
python3 -m venv venv

# Активируйте его
source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate   # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Запуск Backend
Сервер должен работать, чтобы Frontend мог отправлять запросы.

```bash
uvicorn main:app --reload --port 8000
```
> [!NOTE]
> Backend запустится на `http://localhost:8000`. Вы можете проверить интерактивную документацию API по адресу `http://localhost:8000/docs`.

### 3. Запуск Frontend
Поскольку Frontend написан на чистом HTML/JS, его можно открыть напрямую:
1. Откройте файл `frontend/index.html` в любом современном браузере.
2. Или используйте расширение "Live Server" в VS Code.

### 4. Порядок тестирования (User Flow)

#### А. Регистрация и Вход
1. Перейдите на страницу регистрации во вкладке **Регистрация**.
2. Создайте аккаунт (например, `admin@example.com` / `admin123`).
3. После успешной регистрации вы попадете на **Дашборд**.

#### Б. Настройка данных (Справочники)
1. **Клиенты:**
   - Перейдите в раздел "👥 Клиенты".
   - Нажмите "+ Добавить клиента".
   - Заполните данные и сохраните.
2. **Товары:**
   - Перейдите в раздел "📦 Товары".
   - Добавьте несколько товаров с ценами и остатками.

#### В. Работа с заказами
1. **Создание заказа:**
   - Сейчас заказы и платежи отображаются в виде списков. 
   - Вы можете протестировать создание через API (Swagger UI) или добавить функционал создания в UI.
2. **Проверка Дашборда:**
   - После добавления данных обновите страницу.
   - Дашборд отобразит общее количество заказов, выручку и топ клиентов.

---

## 🛠 Технологический стек
- **Backend:** FastAPI, SQLAlchemy (ORM), Pydantic v2.
- **Security:** JWT Authentication, Bcrypt.
- **Database:** SQLite (aiosqlite).
- **Frontend:** HTML5, CSS3 (Modern Dark Theme), Vanilla JavaScript.

## 📁 Структура проекта
- `backend/` — Серверная часть, API и логика БД.
- `frontend/` — Веб-интерфейс (SPA на чистом JS).
- `README.md` — Инструкция по запуску.
